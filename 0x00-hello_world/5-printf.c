#include <stdio.h>
/**
 * main - Printing a text with the fxn printf;
 * Return: 0 (Success);
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
